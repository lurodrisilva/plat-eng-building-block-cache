plat-eng-cache-package:
  team: "test-team"
  environment: "test"
  serviceAccount:
    create: true
    automount: true
    annotations: {}
    name: ""
  providerConfigRef:
    name: "test-provider"
  writeConnectionSecretToRef:
    namespace: "test-ns"
  # Disabled: the block renders nothing by default. The fail-closed path is exercised
  # by redis-cache_test.yaml, which sets caches.redis explicitly.
  caches:
    redis: []
